server:
  port: 8761 # Стандартный порт для Eureka

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # Не регистрировать сервер как клиента
    fetch-registry: false       # Не скачивать реестр (он и есть реестр)
